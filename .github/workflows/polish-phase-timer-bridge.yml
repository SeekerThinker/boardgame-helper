name: Polish phase timer bridge

on:
  push:
    branches:
      - feat/phase-timer-bridge
    paths:
      - .github/tmp/polish-phase-timer-bridge.py
      - .github/workflows/polish-phase-timer-bridge.yml

permissions:
  contents: write

jobs:
  polish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
      - name: Apply polish
        run: python .github/tmp/polish-phase-timer-bridge.py
      - name: Install dependencies
        run: npm ci
      - name: Install Chromium
        run: npx playwright install --with-deps chromium
      - name: Run full check
        run: npm run check
      - name: Commit product tree and remove temporary files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          rm -f .github/tmp/polish-phase-timer-bridge.py .github/workflows/polish-phase-timer-bridge.yml
          git add -A
          git commit -m "test: surface phase timer bridge coverage"
          git push origin HEAD:feat/phase-timer-bridge
