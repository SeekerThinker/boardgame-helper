name: Table OS Final Score Bridge Migration

on:
  push:
    branches:
      - feat/table-os-final-score-bridge-20260912

permissions:
  contents: write

jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: feat/table-os-final-score-bridge-20260912
          fetch-depth: 0
      - name: Apply final-score bridge
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path

          def replace_once(path, old, new):
              p = Path(path)
              text = p.read_text()
              count = text.count(old)
              if count != 1:
                  raise SystemExit(f'{path}: expected one match, got {count}')
              p.write_text(text.replace(old, new, 1))

          # Core: import exact Table OS totals into the mature base score model.
          replace_once('src/core.js',
          """export function recalculateScores(state) {
  state.players.forEach(player => {
    const breakdown = {};
    state.score.fields.forEach(field => { breakdown[field.id] = 0; });
    state.score.rounds.forEach(round => {
      state.score.fields.forEach(field => {
        breakdown[field.id] += roundScoreValue(round, player.id, field.id);
      });
    });
    player.scoreBreakdown = breakdown;
    player.score = state.score.fields.reduce((sum, field) => sum + breakdown[field.id] * field.effect, 0);
  });
}

export function totalScore(state, player) {
""",
          """export function recalculateScores(state) {
  state.players.forEach(player => {
    const breakdown = {};
    state.score.fields.forEach(field => { breakdown[field.id] = 0; });
    state.score.rounds.forEach(round => {
      state.score.fields.forEach(field => {
        breakdown[field.id] += roundScoreValue(round, player.id, field.id);
      });
    });
    player.scoreBreakdown = breakdown;
    player.score = state.score.fields.reduce((sum, field) => sum + breakdown[field.id] * field.effect, 0);
  });
}

export function replaceScoresWithTotals(state, entries, { positiveLabel = 'Table OS score', negativeLabel = 'Table OS deduction' } = {}) {
  if (!Array.isArray(entries) || entries.length !== state.players.length) return false;
  const validPlayers = new Set(state.players.map(player => player.id));
  const byPlayer = new Map();
  for (const entry of entries) {
    const playerId = String(entry?.playerId ?? '');
    const total = Number(entry?.total);
    if (!validPlayers.has(playerId) || byPlayer.has(playerId) || !Number.isFinite(total) || Math.abs(total) > 999999) return false;
    byPlayer.set(playerId, Math.round(total * 100) / 100);
  }
  if (state.players.some(player => !byPlayer.has(player.id))) return false;

  const positive = { id: 'table_os_total', nameKey: '', customName: String(positiveLabel || 'Table OS score').slice(0, 20), step: 1, effect: 1 };
  const hasNegative = state.players.some(player => byPlayer.get(player.id) < 0);
  const negative = { id: 'table_os_deduction', nameKey: '', customName: String(negativeLabel || 'Table OS deduction').slice(0, 20), step: 1, effect: -1 };
  state.score.fields = hasNegative ? [positive, negative] : [positive];
  state.score.rounds = [];
  state.score.history = [];
  state.score.undoStack = [];
  const round = ensureRound(state, state.timer.round);
  state.players.forEach(player => {
    const total = byPlayer.get(player.id);
    round.scores[player.id][positive.id] = Math.max(0, total);
    if (hasNegative) round.scores[player.id][negative.id] = Math.max(0, -total);
  });
  round.updatedAt = new Date().toISOString();
  recalculateScores(state);
  return true;
}

export function totalScore(state, player) {
""")

          # Main app: accept one explicit bridge event, then use the existing Summary/archive path.
          replace_once('src/app.js',
          """  movePlayer, setPlayerColor
} from './core.js';
""",
          """  movePlayer, setPlayerColor, replaceScoresWithTotals
} from './core.js';
""")
          replace_once('src/app.js',
          """setSoundOn(state.settings.soundOn);
setAudioLocale(state.locale);
bindAppEvents(document.getElementById('app'));
""",
          """function applyTableOsFinalScores(event) {
  const detail = event?.detail && typeof event.detail === 'object' ? event.detail : {};
  const applied = replaceScoresWithTotals(state, detail.scores, {
    positiveLabel: detail.positiveLabel,
    negativeLabel: detail.negativeLabel
  });
  if (!applied) return;
  state.activeTool = 'summary';
  render();
}

document.addEventListener('tableos:use-final-scores', applyTableOsFinalScores);

setSoundOn(state.settings.soundOn);
setAudioLocale(state.locale);
bindAppEvents(document.getElementById('app'));
""")

          # Companion: expose the bridge only on the Score Sheet and only for a clean one-to-one main roster.
          replace_once('src/tabletop-companion.js',
          """import { TABLE_OS_STORAGE_KEY } from './tabletop-core.js';
""",
          """import { TABLE_OS_STORAGE_KEY, scoreCardForParticipant } from './tabletop-core.js';
""")
          replace_once('src/tabletop-companion.js',
          """    newSessionDetected: '检测到新的主牌局', startNewSession: '开始新一局', keepTableState: '保留当前桌面',
    newSessionStarted: '已为新牌局重置本局状态。', tableStateKept: '已保留当前桌面状态。'
""",
          """    newSessionDetected: '检测到新的主牌局', startNewSession: '开始新一局', keepTableState: '保留当前桌面',
    newSessionStarted: '已为新牌局重置本局状态。', tableStateKept: '已保留当前桌面状态。',
    useFinalScores: '设为最终成绩', finalScoresConfirm: '将 Table OS 的最终分数同步到主应用，并替换当前主应用计分数据？'
""")
          replace_once('src/tabletop-companion.js',
          """    newSessionDetected: 'New main game detected', startNewSession: 'Start new table', keepTableState: 'Keep table state',
    newSessionStarted: 'Session state reset for the new game.', tableStateKept: 'Current table state kept.'
""",
          """    newSessionDetected: 'New main game detected', startNewSession: 'Start new table', keepTableState: 'Keep table state',
    newSessionStarted: 'Session state reset for the new game.', tableStateKept: 'Current table state kept.',
    useFinalScores: 'Use as final scores', finalScoresConfirm: 'Send the Table OS totals to the main app and replace its current score data?'
""")
          replace_once('src/tabletop-companion.js',
          """function mainGameContext() {
""",
          """function finalScorePayload(game) {
  const table = readTableOsState();
  const players = Array.isArray(game?.players) ? game.players : [];
  const participants = Array.isArray(table?.participants) ? table.participants : [];
  const fields = Array.isArray(table?.scoreSheet?.fields) ? table.scoreSheet.fields : [];
  const rows = table?.scoreSheet?.values && typeof table.scoreSheet.values === 'object'
    ? Object.values(table.scoreSheet.values) : [];
  if (!players.length || !fields.length || game?.session?.status === 'finished') return null;
  if (!rows.some(values => values && typeof values === 'object' && Object.keys(values).length)) return null;

  const bySource = new Map();
  for (const participant of participants) {
    if (!participant?.sourcePlayerId) continue;
    const sourceId = String(participant.sourcePlayerId);
    if (bySource.has(sourceId)) return null;
    bySource.set(sourceId, participant);
  }

  const scores = [];
  for (const player of players) {
    const participant = bySource.get(String(player?.id ?? ''));
    if (!participant) return null;
    const card = scoreCardForParticipant(table, participant.id);
    const total = Number(card?.total);
    if (!Number.isFinite(total) || Math.abs(total) > 999999) return null;
    scores.push({ playerId: String(player.id), total: Math.round(total * 100) / 100 });
  }
  return {
    scores,
    positiveLabel: locale() === 'en' ? 'Table OS score' : 'Table OS 得分',
    negativeLabel: locale() === 'en' ? 'Table OS deduction' : 'Table OS 扣分'
  };
}

function mainGameContext() {
""")
          replace_once('src/tabletop-companion.js',
          """function openMainFlow() {
  document.querySelector('.tableos-sheet [data-os-action=\"close\"]')?.click();
  queueMicrotask(() => document.querySelector('[data-tab=\"flow\"]')?.click());
}

function enhanceAccessibility(sheet) {
""",
          """function openMainFlow() {
  document.querySelector('.tableos-sheet [data-os-action=\"close\"]')?.click();
  queueMicrotask(() => document.querySelector('[data-tab=\"flow\"]')?.click());
}

function transferFinalScores() {
  const payload = finalScorePayload(readGameState());
  if (!payload || !confirm(tr('finalScoresConfirm'))) return false;
  document.querySelector('.tableos-sheet [data-os-action=\"close\"]')?.click();
  queueMicrotask(() => document.dispatchEvent(new CustomEvent('tableos:use-final-scores', { detail: payload })));
  return true;
}

function enhanceAccessibility(sheet) {
""")
          replace_once('src/tabletop-companion.js',
          """function companionMarkup(context) {
""",
          """function companionMarkup(context, finalScores = null) {
""")
          replace_once('src/tabletop-companion.js',
          """  const actions = `${lastUndo ? `<button type=\"button\" class=\"tableos-btn\" data-tableos-companion-action=\"undo\" title=\"${esc(lastUndo.label)}\">↶ ${esc(tr('undo'))}</button>` : ''}${sessionActions}${context ? `<button type=\"button\" class=\"tableos-btn\" data-tableos-companion-action=\"flow\">${esc(tr('openFlow'))}</button>` : ''}`;
""",
          """  const actions = `${lastUndo ? `<button type=\"button\" class=\"tableos-btn\" data-tableos-companion-action=\"undo\" title=\"${esc(lastUndo.label)}\">↶ ${esc(tr('undo'))}</button>` : ''}${sessionActions}${finalScores ? `<button type=\"button\" class=\"tableos-btn primary\" data-tableos-companion-action=\"final-score\">${esc(tr('useFinalScores'))}</button>` : ''}${context ? `<button type=\"button\" class=\"tableos-btn\" data-tableos-companion-action=\"flow\">${esc(tr('openFlow'))}</button>` : ''}`;
""")
          replace_once('src/tabletop-companion.js',
          """  const context = mainGameContext();
  if (context?.newSession) lastUndo = null;
  let bar = sheet.querySelector('.tableos-companion');
""",
          """  const context = mainGameContext();
  if (context?.newSession) lastUndo = null;
  const section = activeSection();
  const finalScores = context && !context.newSession && !context.rosterDrift && section === 'score'
    ? finalScorePayload(readGameState()) : null;
  let bar = sheet.querySelector('.tableos-companion');
""")
          replace_once('src/tabletop-companion.js',
          """  const signature = JSON.stringify({ context, undo: lastUndo?.label || '', notice, lang: locale(), playing: inPlayMode() });
  if (bar.dataset.signature !== signature) {
    bar.dataset.signature = signature;
    bar.innerHTML = companionMarkup(context);
""",
          """  const signature = JSON.stringify({ context, undo: lastUndo?.label || '', notice, lang: locale(), playing: inPlayMode(), section, finalScores: finalScores?.scores || null });
  if (bar.dataset.signature !== signature) {
    bar.dataset.signature = signature;
    bar.innerHTML = companionMarkup(context, finalScores);
""")
          replace_once('src/tabletop-companion.js',
          """    if (action === 'keep-session') keepCurrentTableState();
    if (action === 'flow') openMainFlow();
""",
          """    if (action === 'keep-session') keepCurrentTableState();
    if (action === 'final-score') transferFinalScores();
    if (action === 'flow') openMainFlow();
""")

          # Archive: Table OS formulas can produce decimals, so finished-game history must not round them away.
          replace_once('src/archive.js',
          """function safeColor(value) {
  return /^#[0-9a-f]{6}$/i.test(value || '') ? value : '#64748b';
}
""",
          """function safeColor(value) {
  return /^#[0-9a-f]{6}$/i.test(value || '') ? value : '#64748b';
}

function safeScore(value) {
  const number = Number(value);
  if (!Number.isFinite(number)) return 0;
  return Math.round(Math.min(1000000, Math.max(-1000000, number)) * 100) / 100;
}
""")
          replace_once('src/archive.js',
          """      score: clampInt(player?.score, -1000000, 1000000, 0),
""",
          """      score: safeScore(player?.score),
""")

          # Unit coverage for exact signed totals and decimal archive scores.
          replace_once('tests/unit/core.test.js',
          """  shufflePlayerOrder, movePlayer, setPlayerColor, activePlayer
} from '../../src/core.js';
""",
          """  shufflePlayerOrder, movePlayer, setPlayerColor, activePlayer, replaceScoresWithTotals
} from '../../src/core.js';
""")
          replace_once('tests/unit/core.test.js',
          """test('preset replacement creates the correct low-score model', () => {
""",
          """test('final-score bridge replaces base score data with exact signed totals', () => {
  const state = createDefaultState();
  const [a, b, c, d] = state.players;
  ensureRound(state, 1);
  setRoundScore(state, 1, a.id, 'vp', 99);
  const applied = replaceScoresWithTotals(state, [
    { playerId: a.id, total: 13.5 },
    { playerId: b.id, total: -4.25 },
    { playerId: c.id, total: 0 },
    { playerId: d.id, total: 7 }
  ], { positiveLabel: 'Advanced score', negativeLabel: 'Advanced deduction' });
  assert.equal(applied, true);
  assert.deepEqual(state.score.fields.map(field => field.effect), [1, -1]);
  assert.equal(a.score, 13.5);
  assert.equal(b.score, -4.25);
  assert.equal(c.score, 0);
  assert.equal(d.score, 7);
  assert.equal(state.score.history.length, 0);
  assert.equal(state.score.undoStack.length, 0);
  const snapshot = JSON.stringify(state.score);
  assert.equal(replaceScoresWithTotals(state, [{ playerId: a.id, total: 1 }]), false, 'partial mappings are rejected');
  assert.equal(JSON.stringify(state.score), snapshot, 'invalid bridge payloads do not mutate scoring');
});

test('preset replacement creates the correct low-score model', () => {
""")
          replace_once('tests/unit/archive.test.js',
          """test('save prepends, dedupes by id, and persists to storage', () => {
""",
          """test('archive preserves fractional final scores', () => {
  const entry = normalizeArchiveEntry({ ...sampleGame, players: [{ name: 'Alice', color: '#f97316', score: 13.75, rank: 1 }] });
  assert.equal(entry.players[0].score, 13.75);
});

test('save prepends, dedupes by id, and persists to storage', () => {
""")

          # Browser regression: calculate advanced totals, bridge them, finish, and prove archive keeps decimals.
          replace_once('tests/e2e/run-table-os-companion-e2e.js',
          """async function rematchMainGame(page) {
""",
          """async function runFinalScoreBridgeFlow() {
  const context = await browser.newContext({ viewport: { width: 390, height: 844 }, locale: 'zh-CN' });
  const page = await context.newPage();
  const errors = [];
  page.on('console', message => { if (message.type() === 'error') errors.push(message.text()); });
  page.on('pageerror', error => errors.push(error.message));
  page.on('dialog', dialog => dialog.accept());

  await page.goto(url, { waitUntil: 'networkidle' });
  await page.evaluate(() => localStorage.clear());
  await page.reload({ waitUntil: 'networkidle' });
  await page.locator('[data-action=\"start-session\"]').click();
  await openTableOs(page);
  await page.getByRole('button', { name: '编辑配置', exact: true }).click();
  await page.locator('[data-os-template]').selectOption('engine-score');
  await page.getByRole('button', { name: '应用模板', exact: true }).click();
  await page.getByRole('button', { name: '计分表', exact: true }).click();

  const inputs = page.locator('[data-os-score-value]');
  for (const [index, value] of [[0, '12.5'], [1, '4'], [2, '0'], [3, '3'], [4, '1.25'], [5, '0'], [6, '0'], [7, '5.5']]) {
    await inputs.nth(index).fill(value);
    await inputs.nth(index).blur();
  }

  const bridge = page.locator('[data-tableos-companion-action=\"final-score\"]');
  await bridge.waitFor({ state: 'visible' });
  await bridge.click();
  await page.locator('#tableos-root').waitFor({ state: 'detached' });

  const main = await page.evaluate(() => JSON.parse(localStorage.getItem('board-game-assistant-state-v2')));
  assert.equal(main.activeTool, 'summary', 'bridge lands on the mature main summary');
  assert.equal(main.players[0].score, 13.5, 'positive advanced total is preserved exactly');
  assert.equal(main.players[1].score, -4.25, 'negative advanced total is preserved exactly');
  assert.deepEqual(main.score.fields.map(field => field.effect), [1, -1], 'negative totals use an explicit deduction field');
  assert.ok((await page.locator('[data-tab=\"summary\"]').getAttribute('class') || '').includes('active'));

  await page.locator('[data-action=\"finish-session\"]').click();
  const archive = await page.evaluate(() => JSON.parse(localStorage.getItem('board-game-assistant-archive-v1')));
  assert.equal(archive[0].players.find(player => player.name === '玩家 1')?.score, 13.5, 'archive keeps fractional Table OS final scores');
  assert.equal(archive[0].players.find(player => player.name === '玩家 2')?.score, -4.25, 'archive keeps negative fractional Table OS final scores');
  assert.equal(errors.length, 0, `Table OS final-score bridge console errors: ${errors.join(' | ')}`);
  await context.close();
}

async function rematchMainGame(page) {
""")
          replace_once('tests/e2e/run-table-os-companion-e2e.js',
          """  await runPrimaryFlow();
  await runQuietLifecycleFlow();
""",
          """  await runPrimaryFlow();
  await runFinalScoreBridgeFlow();
  await runQuietLifecycleFlow();
""")
          replace_once('tests/e2e/run-table-os-companion-e2e.js',
          """      'ordinary phase undo', 'main-timer bridge', 'contextual accessibility labels',
""",
          """      'ordinary phase undo', 'main-timer bridge', 'final-score bridge', 'contextual accessibility labels',
""")

          # Product documentation: this bridge is now implemented, not a future idea.
          replace_once('TABLE_OS.md',
          """- 牌局模式只显示玩家输入和结果；公式/key 等结构配置只在编辑模式出现。
""",
          """- 牌局模式只显示玩家输入和结果；公式/key 等结构配置只在编辑模式出现。
- 当主对局玩家与 Table OS 参与者能一一对应时，计分表页可以一键“设为最终成绩”，把高级总分交给主应用既有的排名、分享、归档和复赛流程；不会建立持续双向同步。
""")
          replace_once('TABLE_OS.md',
          """1. Phase Engine 可选绑定主计时器的阶段时长，而不是复制一套计时器。
2. 高级 ScoreSheet 可选把最终总分写入基础结算，而不是再造一个结算页。
3. 历史归档可附带 Table OS 摘要/快照，而不是把完整高级状态塞进基础 schema。
4. 常用自定义配置可以保存为“我的模板”，但模板管理必须保持轻量。
""",
          """1. Phase Engine 可选绑定主计时器的阶段时长，而不是复制一套计时器。
2. 历史归档可附带 Table OS 摘要/快照，而不是把完整高级状态塞进基础 schema。
3. 常用自定义配置可以保存为“我的模板”，但模板管理必须保持轻量。
""")
          PY
      - name: Commit generated bridge changes
        shell: bash
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add src/core.js src/app.js src/tabletop-companion.js src/archive.js tests/unit/core.test.js tests/unit/archive.test.js tests/e2e/run-table-os-companion-e2e.js TABLE_OS.md
          git rm .github/workflows/table-os-score-bridge-migrate.yml
          git commit -m "feat: bridge Table OS totals into final results"
          git push origin HEAD:feat/table-os-final-score-bridge-20260912
