name: Temporary My Templates Migration

on:
  push:
    branches:
      - feat/table-os-my-templates-20260913

permissions:
  contents: write

jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
      - name: Apply focused My Templates changes
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path

          def replace_once(path, old, new):
              p = Path(path)
              text = p.read_text()
              if old not in text:
                  raise SystemExit(f'anchor not found in {path}: {old[:120]!r}')
              if text.count(old) != 1:
                  raise SystemExit(f'anchor not unique in {path}: {old[:120]!r}')
              p.write_text(text.replace(old, new, 1))

          # tabletop-core.js: privacy-safe structural user templates.
          replace_once('src/tabletop-core.js',
              "export const MAX_CAMPAIGN_FLAGS = 40;\n",
              "export const MAX_CAMPAIGN_FLAGS = 40;\nexport const MAX_USER_TEMPLATES = 12;\nexport const USER_TEMPLATE_VERSION = 1;\n")

          replace_once('src/tabletop-core.js',
              "export function templateById(id) {\n  return ASSISTANT_TEMPLATES.find(template => template.id === id) || ASSISTANT_TEMPLATES[0];\n}\n",
              "export function templateById(id) {\n  return ASSISTANT_TEMPLATES.find(template => template.id === id) || ASSISTANT_TEMPLATES[0];\n}\n\nfunction normalizeAppliedTemplateId(value) {\n  const raw = String(value ?? '').trim().slice(0, 72);\n  if (/^user:[a-zA-Z0-9_-]{1,64}$/.test(raw)) return raw;\n  return templateById(raw).id;\n}\n")

          replace_once('src/tabletop-core.js',
              "    appliedTemplateId: templateById(input.appliedTemplateId).id,\n",
              "    appliedTemplateId: normalizeAppliedTemplateId(input.appliedTemplateId),\n")

          anchor = "function resetTemplateModules(state) {\n"
          insert = r'''export function normalizeUserTemplate(input) {
  const source = input && typeof input === 'object' ? input : {};
  const trackerSource = Array.isArray(source.trackers) ? source.trackers : [];
  const phaseSource = Array.isArray(source.phases) ? source.phases : Array.isArray(source.phases?.items) ? source.phases.items : [];
  const teamSource = Array.isArray(source.teams) ? source.teams : [];
  const scoreSource = Array.isArray(source.scoreFields) ? source.scoreFields : Array.isArray(source.scoreSheet?.fields) ? source.scoreSheet.fields : [];
  const usedKeys = new Set();
  return {
    version: USER_TEMPLATE_VERSION,
    id: identifier(source.id, 'ut_'),
    name: text(source.name || 'My template', 48),
    trackers: trackerSource.slice(0, MAX_TRACKERS).map((tracker, index) => {
      const normalized = trackerFromTemplate({
        ...tracker,
        value: tracker?.initial ?? tracker?.value,
        values: {}
      }, index);
      return {
        name: normalized.name,
        scope: normalized.scope,
        persistence: normalized.persistence,
        min: normalized.min,
        max: normalized.max,
        step: normalized.step,
        initial: normalized.initial
      };
    }),
    phases: phaseSource.slice(0, MAX_PHASES).map((phase, index) => ({
      name: text(phase?.name || `Phase ${index + 1}`, 32),
      note: text(phase?.note, 120)
    })),
    teams: teamSource.slice(0, MAX_TEAMS).map((team, index) => ({
      name: text(team?.name || `Team ${index + 1}`, 28),
      color: color(team?.color, index)
    })),
    scoreFields: scoreSource.slice(0, MAX_SCORE_FIELDS).map((field, index) => {
      const normalized = scoreFieldFromTemplate(field, index, usedKeys);
      return {
        key: normalized.key,
        name: normalized.name,
        kind: normalized.kind,
        formula: normalized.formula,
        step: normalized.step,
        effect: normalized.effect,
        includeInTotal: normalized.includeInTotal
      };
    }),
    campaignEnabled: Boolean(source.campaignEnabled)
  };
}

export function createUserTemplateFromState(state, name = 'My template') {
  return normalizeUserTemplate({
    id: uid('ut_'),
    name,
    trackers: state.trackers,
    phases: state.phases.items,
    teams: state.teams,
    scoreFields: state.scoreSheet.fields,
    campaignEnabled: state.campaign.enabled
  });
}

'''
          replace_once('src/tabletop-core.js', anchor, insert + anchor)

          replace_once('src/tabletop-core.js',
              "export function applyAssistantTemplate(state, templateId, { preserveParticipants = true, locale = null } = {}) {\n",
              r'''export function applyUserTemplate(state, input, { preserveParticipants = true } = {}) {
  const template = normalizeUserTemplate(input);
  const participants = preserveParticipants ? state.participants.slice() : [];
  resetTemplateModules(state);
  state.participants = participants;
  state.appliedTemplateId = `user:${template.id}`;
  state.phases.items = template.phases.map(phase => ({ id: uid('phase_'), name: phase.name, note: phase.note }));
  state.trackers = template.trackers.map((tracker, index) => trackerFromTemplate({
    ...tracker,
    value: tracker.initial,
    values: {}
  }, index));
  const usedKeys = new Set();
  state.scoreSheet.fields = template.scoreFields.map((field, index) => scoreFieldFromTemplate(field, index, usedKeys));
  template.teams.forEach((savedTeam, index) => {
    const team = addTeam(state, savedTeam.name || `Team ${index + 1}`);
    if (team) team.color = color(savedTeam.color, index);
  });
  if (template.campaignEnabled) state.campaign.enabled = true;
  state.ui.activeSection = 'overview';
  state.ui.mode = 'play';
  touch(state);
  return state;
}

export function applyAssistantTemplate(state, templateId, { preserveParticipants = true, locale = null } = {}) {
''')

          # tabletop.js: local template shelf + save/apply/rename/delete UI.
          replace_once('src/tabletop.js',
              "  TABLE_OS_STORAGE_KEY, ASSISTANT_TEMPLATES, MAX_TABLE_OS_PARTICIPANTS, MAX_TRACKERS, MAX_PHASES,\n  MAX_TEAMS, MAX_SCORE_FIELDS, MAX_CAMPAIGN_FLAGS, createDefaultTableOsState, normalizeTableOsState,\n",
              "  TABLE_OS_STORAGE_KEY, ASSISTANT_TEMPLATES, MAX_TABLE_OS_PARTICIPANTS, MAX_TRACKERS, MAX_PHASES,\n  MAX_TEAMS, MAX_SCORE_FIELDS, MAX_CAMPAIGN_FLAGS, MAX_USER_TEMPLATES, uid, createDefaultTableOsState, normalizeTableOsState,\n")
          replace_once('src/tabletop.js',
              "  addCampaignFlag, toggleCampaignFlag, removeCampaignFlag,\n  applyAssistantTemplate, resetTableOsSession, serializeTableOsState, parseTableOsState, touch\n",
              "  addCampaignFlag, toggleCampaignFlag, removeCampaignFlag,\n  normalizeUserTemplate, createUserTemplateFromState, applyUserTemplate,\n  applyAssistantTemplate, resetTableOsSession, serializeTableOsState, parseTableOsState, touch\n")
          replace_once('src/tabletop.js',
              "const LEGACY_GAME_STATE_KEY = 'board-game-assistant-state-v1';\n",
              "const LEGACY_GAME_STATE_KEY = 'board-game-assistant-state-v1';\nconst USER_TEMPLATES_STORAGE_KEY = 'board-game-assistant-table-os-user-templates-v1';\n")

          replace_once('src/tabletop.js',
              "    templateHint: '模板只是可编辑的工作流起点，不替代官方规则。', moderatorNoteHidden: '主持备注不会显示给玩家。'\n",
              "    templateHint: '模板只是可编辑的工作流起点，不替代官方规则。', moderatorNoteHidden: '主持备注不会显示给玩家。',\n    myTemplates: '我的模板', saveMyTemplate: '保存当前配置', applyMyTemplate: '应用我的模板', renameMyTemplate: '重命名', deleteMyTemplate: '删除模板',\n    myTemplateHint: '只保存在本机，只保存追踪器、阶段、团队结构和计分公式；不保存玩家、身份、当前数值或战役内容。', myTemplateEmpty: '还没有我的模板', myTemplateDefault: '我的模板', customSetup: '自定义配置',\n    myTemplateNamePrompt: '模板名称', myTemplateSaved: '已保存为我的模板。', myTemplateRenamed: '模板已重命名。', myTemplateDeleted: '模板已删除。', myTemplateLimit: '我的模板最多保存 12 个。',\n    confirmMyTemplate: '应用我的模板会重置追踪器、阶段、团队、身份和高级计分表，但保留参与者与战役记忆。继续吗？', confirmDeleteMyTemplate: '删除这个本机模板吗？'\n")
          replace_once('src/tabletop.js',
              "    templateHint: 'Templates are editable workflow starters, not replacements for official rules.', moderatorNoteHidden: 'Moderator notes are never shown in player reveal.'\n",
              "    templateHint: 'Templates are editable workflow starters, not replacements for official rules.', moderatorNoteHidden: 'Moderator notes are never shown in player reveal.',\n    myTemplates: 'My templates', saveMyTemplate: 'Save current setup', applyMyTemplate: 'Apply my template', renameMyTemplate: 'Rename', deleteMyTemplate: 'Delete template',\n    myTemplateHint: 'Stored only on this device. Saves tracker, phase, team structure and score formulas — never players, roles, live values or campaign content.', myTemplateEmpty: 'No saved templates yet', myTemplateDefault: 'My template', customSetup: 'Custom setup',\n    myTemplateNamePrompt: 'Template name', myTemplateSaved: 'Saved to My templates.', myTemplateRenamed: 'Template renamed.', myTemplateDeleted: 'Template deleted.', myTemplateLimit: 'My templates can store up to 12 setups.',\n    confirmMyTemplate: 'Applying My template resets trackers, phases, teams, roles and the advanced score sheet while preserving participants and campaign memory. Continue?', confirmDeleteMyTemplate: 'Delete this local template?'\n")

          replace_once('src/tabletop.js',
              "let state = loadState();\nlet isOpen = false;\nlet selectedTemplateId = state.appliedTemplateId || 'universal';\n",
              "let state = loadState();\nlet isOpen = false;\nlet selectedTemplateId = ASSISTANT_TEMPLATES.some(template => template.id === state.appliedTemplateId) ? state.appliedTemplateId : 'universal';\nlet userTemplates = loadUserTemplates();\nlet selectedUserTemplateId = userTemplates[0]?.id || '';\n")

          anchor = "function persist() {\n  try { localStorage.setItem(TABLE_OS_STORAGE_KEY, JSON.stringify(state)); } catch (_) {}\n}\n"
          insert = r'''
function loadUserTemplates() {
  try {
    const raw = localStorage.getItem(USER_TEMPLATES_STORAGE_KEY);
    const parsed = raw ? JSON.parse(raw) : [];
    if (!Array.isArray(parsed)) return [];
    const used = new Set();
    return parsed.slice(0, MAX_USER_TEMPLATES).map(item => {
      let template = normalizeUserTemplate(item);
      if (used.has(template.id)) template = { ...template, id: uid('ut_') };
      used.add(template.id);
      return template;
    });
  } catch (_) {
    return [];
  }
}

function persistUserTemplates() {
  try { localStorage.setItem(USER_TEMPLATES_STORAGE_KEY, JSON.stringify(userTemplates.slice(0, MAX_USER_TEMPLATES))); } catch (_) {}
}
'''
          replace_once('src/tabletop.js', anchor, anchor + insert)

          start = "function renderEditorToolbar() {\n"
          end = "\nfunction renderSection() {\n"
          p = Path('src/tabletop.js')
          text = p.read_text()
          a = text.index(start)
          b = text.index(end, a)
          replacement = r'''function renderEditorToolbar() {
  const selectedUserTemplate = userTemplates.find(template => template.id === selectedUserTemplateId) || null;
  const hasUserTemplates = userTemplates.length > 0;
  const selectedBuiltIn = ASSISTANT_TEMPLATES.find(template => template.id === selectedTemplateId) || ASSISTANT_TEMPLATES[0];
  return `<div class="tableos-toolbar">
    <label class="tableos-template"><span>${esc(tr('template'))}</span>
      <select data-os-template>${ASSISTANT_TEMPLATES.map(template => `<option value="${template.id}" ${selectedTemplateId === template.id ? 'selected' : ''}>${esc(templateName(template))}</option>`).join('')}</select>
    </label>
    <button type="button" class="tableos-btn primary" data-os-action="apply-template">${esc(tr('apply'))}</button>
    <button type="button" class="tableos-btn" data-os-action="sync">${esc(tr('sync'))}</button>
    <label class="tableos-template"><span>${esc(tr('myTemplates'))} · ${userTemplates.length}/${MAX_USER_TEMPLATES}</span>
      <select data-os-user-template ${hasUserTemplates ? '' : 'disabled'}>${hasUserTemplates ? userTemplates.map(template => `<option value="${attr(template.id)}" ${selectedUserTemplate?.id === template.id ? 'selected' : ''}>${esc(template.name)}</option>`).join('') : `<option value="">${esc(tr('myTemplateEmpty'))}</option>`}</select>
    </label>
    <button type="button" class="tableos-btn primary" data-os-action="save-my-template">${esc(tr('saveMyTemplate'))}</button>
    <button type="button" class="tableos-btn" data-os-action="apply-my-template" ${hasUserTemplates ? '' : 'disabled'}>${esc(tr('applyMyTemplate'))}</button>
    <button type="button" class="tableos-btn" data-os-action="rename-my-template" ${hasUserTemplates ? '' : 'disabled'}>${esc(tr('renameMyTemplate'))}</button>
    <button type="button" class="tableos-btn danger" data-os-action="delete-my-template" ${hasUserTemplates ? '' : 'disabled'}>${esc(tr('deleteMyTemplate'))}</button>
    <button type="button" class="tableos-btn" data-os-action="export">${esc(tr('export'))}</button>
    <button type="button" class="tableos-btn" data-os-action="import">${esc(tr('import'))}</button>
    <input id="tableos-import-file" type="file" accept="application/json,.json" hidden>
    <p class="tableos-template-note">${esc(templateDescription(selectedBuiltIn))} · ${esc(tr('templateHint'))}</p>
    <p class="tableos-template-note">${esc(tr('myTemplateHint'))}</p>
  </div>`;
}
'''
          p.write_text(text[:a] + replacement + text[b:])

          replace_once('src/tabletop.js',
              "  const template = ASSISTANT_TEMPLATES.find(item => item.id === state.appliedTemplateId) || ASSISTANT_TEMPLATES[0];\n  const activePhase = state.phases.items[state.phases.activeIndex];\n",
              "  const userTemplateId = state.appliedTemplateId.startsWith('user:') ? state.appliedTemplateId.slice(5) : '';\n  const userTemplate = userTemplates.find(item => item.id === userTemplateId) || null;\n  const template = ASSISTANT_TEMPLATES.find(item => item.id === state.appliedTemplateId) || ASSISTANT_TEMPLATES[0];\n  const activeTemplateName = userTemplate?.name || (userTemplateId ? tr('customSetup') : templateName(template));\n  const activeTemplateDescription = userTemplate ? tr('myTemplateHint') : (userTemplateId ? tr('templateHint') : templateDescription(template));\n  const activePhase = state.phases.items[state.phases.activeIndex];\n")
          replace_once('src/tabletop.js',
              "        <span>${esc(tr('activeTemplate'))}</span><h3>${esc(templateName(template))}</h3><p>${esc(templateDescription(template))}</p>\n",
              "        <span>${esc(tr('activeTemplate'))}</span><h3>${esc(activeTemplateName)}</h3><p>${esc(activeTemplateDescription)}</p>\n")

          anchor = "function adoptRandomTeams() {\n"
          insert = r'''function saveCurrentUserTemplate() {
  flushActiveDraft();
  if (userTemplates.length >= MAX_USER_TEMPLATES) { flash(tr('myTemplateLimit')); return; }
  const name = prompt(tr('myTemplateNamePrompt'), tr('myTemplateDefault'));
  if (!name?.trim()) return;
  const template = createUserTemplateFromState(state, name.trim());
  userTemplates.unshift(template);
  selectedUserTemplateId = template.id;
  persistUserTemplates();
  flash(tr('myTemplateSaved'));
}

function applySelectedUserTemplate() {
  const template = userTemplates.find(item => item.id === selectedUserTemplateId);
  if (!template) return;
  if (hasConfiguredWorkspace() && !confirm(tr('confirmMyTemplate'))) return;
  applyUserTemplate(state, template, { preserveParticipants: true });
  persist();
  render();
}

function renameSelectedUserTemplate() {
  const index = userTemplates.findIndex(item => item.id === selectedUserTemplateId);
  if (index < 0) return;
  const name = prompt(tr('myTemplateNamePrompt'), userTemplates[index].name);
  if (!name?.trim()) return;
  userTemplates[index] = normalizeUserTemplate({ ...userTemplates[index], name: name.trim() });
  persistUserTemplates();
  flash(tr('myTemplateRenamed'));
}

function deleteSelectedUserTemplate() {
  const index = userTemplates.findIndex(item => item.id === selectedUserTemplateId);
  if (index < 0 || !confirm(tr('confirmDeleteMyTemplate'))) return;
  userTemplates.splice(index, 1);
  selectedUserTemplateId = userTemplates[Math.min(index, userTemplates.length - 1)]?.id || '';
  persistUserTemplates();
  flash(tr('myTemplateDeleted'));
}

'''
          replace_once('src/tabletop.js', anchor, insert + anchor)

          replace_once('src/tabletop.js',
              "    if (d.osAction === 'sync') { syncParticipantsFromGame(state, readGamePlayers()); persist(); flash(tr('synced')); return; }\n",
              "    if (d.osAction === 'sync') { syncParticipantsFromGame(state, readGamePlayers()); persist(); flash(tr('synced')); return; }\n    if (d.osAction === 'save-my-template') { saveCurrentUserTemplate(); return; }\n    if (d.osAction === 'apply-my-template') { applySelectedUserTemplate(); return; }\n    if (d.osAction === 'rename-my-template') { renameSelectedUserTemplate(); return; }\n    if (d.osAction === 'delete-my-template') { deleteSelectedUserTemplate(); return; }\n")
          replace_once('src/tabletop.js',
              "    if (d.osTemplate !== undefined) { selectedTemplateId = element.value; render(); return; }\n",
              "    if (d.osTemplate !== undefined) { selectedTemplateId = element.value; render(); return; }\n    if (d.osUserTemplate !== undefined) { selectedUserTemplateId = element.value; render(); return; }\n")
          replace_once('src/tabletop.js',
              "    selectedTemplateId = state.appliedTemplateId;\n",
              "    selectedTemplateId = ASSISTANT_TEMPLATES.some(template => template.id === state.appliedTemplateId) ? state.appliedTemplateId : 'universal';\n")

          # Unit coverage: snapshots are structural and applying them is non-destructive to campaign memory.
          replace_once('tests/unit/tabletop-core.test.js',
              "  addCampaignFlag, toggleCampaignFlag, applyAssistantTemplate, resetTableOsSession,\n  serializeTableOsState, parseTableOsState\n",
              "  addCampaignFlag, toggleCampaignFlag, applyAssistantTemplate, resetTableOsSession,\n  createUserTemplateFromState, applyUserTemplate, normalizeUserTemplate, serializeTableOsState, parseTableOsState\n")

          unit_anchor = "test('universal trackers support scopes, clamps and explicit persistence', () => {\n"
          unit_test = r'''test('My Templates copy reusable structure without live, private, roster or campaign content', () => {
  const state = createDefaultTableOsState();
  const player = addParticipant(state, 'Alice');
  applyAssistantTemplate(state, 'coop-crisis');
  const team = state.teams[0];
  toggleTeamMember(state, team.id, player.id);
  setRole(state, player.id, { role: 'Secret Seer', faction: 'Town', note: 'Moderator secret', secret: true });
  setTrackerValue(state, state.trackers[0].id, 'global', 8);
  setScoreSheetValue(state, player.id, state.scoreSheet.fields[0].id, 11);
  state.campaign.enabled = true;
  state.campaign.name = 'Private campaign';
  state.campaign.chapter = 'Secret chapter';
  state.campaign.notes = 'Long private note';
  const flag = addCampaignFlag(state, 'Unlocked hidden gate');
  toggleCampaignFlag(state, flag.id);

  const saved = createUserTemplateFromState(state, 'Friday setup');
  const raw = JSON.stringify(saved);
  assert.equal(saved.name, 'Friday setup');
  assert.equal(saved.trackers[0].initial, state.trackers[0].initial, 'tracker initial value is configuration');
  assert.equal('values' in saved.trackers[0], false, 'live tracker values are excluded');
  assert.equal('memberIds' in saved.teams[0], false, 'team assignments are excluded');
  assert.equal('participants' in saved, false, 'roster is excluded');
  assert.equal('roles' in saved, false, 'private role assignments are excluded');
  assert.equal('campaign' in saved, false, 'campaign content is excluded');
  assert.equal(raw.includes('Secret Seer'), false);
  assert.equal(raw.includes('Moderator secret'), false);
  assert.equal(raw.includes('Private campaign'), false);
  assert.equal(raw.includes('Unlocked hidden gate'), false);

  const target = createDefaultTableOsState();
  const targetPlayer = addParticipant(target, 'Bob');
  target.campaign.enabled = true;
  target.campaign.name = 'Keep this campaign';
  target.campaign.notes = 'Keep this note';
  const targetFlag = addCampaignFlag(target, 'Keep checkpoint');
  toggleCampaignFlag(target, targetFlag.id);
  applyUserTemplate(target, normalizeUserTemplate(saved));

  assert.deepEqual(participantNames(target), ['Bob'], 'current roster is preserved');
  assert.equal(target.appliedTemplateId, `user:${saved.id}`);
  assert.equal(target.trackers[0].values && Object.keys(target.trackers[0].values).length, 0, 'tracker values restart from initial');
  assert.equal(target.phases.activeIndex, 0);
  assert.equal(target.phases.cycle, 1);
  assert.deepEqual(target.teams[0].memberIds, [], 'saved team structure has no old members');
  assert.equal(target.roles.length, 0);
  assert.deepEqual(target.scoreSheet.values, {});
  assert.equal(target.campaign.name, 'Keep this campaign');
  assert.equal(target.campaign.notes, 'Keep this note');
  assert.equal(target.campaign.flags[0].checked, true, 'campaign memory is untouched by template apply');
  assert.equal(target.participants[0].id, targetPlayer.id);
  assert.equal(normalizeTableOsState(target).appliedTemplateId, `user:${saved.id}`, 'custom template identity survives state normalization');
});

'''
          replace_once('tests/unit/tabletop-core.test.js', unit_anchor, unit_test + unit_anchor)

          # E2E: local persistence, rename/apply/delete, and privacy/transient exclusions.
          replace_once('tests/e2e/run-table-os-e2e.js',
              "  const errors = [];\n  page.on('console', message => { if (message.type() === 'error') errors.push(message.text()); });\n  page.on('pageerror', error => errors.push(error.message));\n  page.on('dialog', dialog => dialog.accept());\n",
              "  const errors = [];\n  const promptResponses = [];\n  page.on('console', message => { if (message.type() === 'error') errors.push(message.text()); });\n  page.on('pageerror', error => errors.push(error.message));\n  page.on('dialog', async dialog => {\n    if (dialog.type() === 'prompt') await dialog.accept(promptResponses.shift() || dialog.defaultValue() || '');\n    else await dialog.accept();\n  });\n")

          e2e_anchor = "  // English UI follows the main language dynamically.\n"
          e2e_test = r'''  // My Templates persist locally but only copy reusable structure, never live/private/campaign data.
  await editMode(page);
  await page.getByRole('button', { name: '团队与身份', exact: true }).click();
  await page.getByRole('button', { name: '添加团队' }).click();
  await page.locator('[data-os-team-member]').first().check();
  const privateTemplateRole = page.locator('[data-os-role-name]').first();
  await privateTemplateRole.fill('不应保存的私密角色'); await privateTemplateRole.blur();
  await page.getByRole('button', { name: '追踪器', exact: true }).click();
  const liveRound = page.locator('[data-os-tracker-value]').first();
  await liveRound.fill('7'); await liveRound.blur();
  promptResponses.push('引擎夜');
  await page.getByRole('button', { name: '保存当前配置' }).click();
  const savedTemplates = await page.evaluate(() => JSON.parse(localStorage.getItem('board-game-assistant-table-os-user-templates-v1') || '[]'));
  assert.equal(savedTemplates.length, 1);
  assert.equal(savedTemplates[0].name, '引擎夜');
  assert.equal(savedTemplates[0].trackers[0].initial, 1, 'template keeps tracker initial configuration');
  assert.equal('values' in savedTemplates[0].trackers[0], false, 'template excludes live tracker values');
  assert.equal('memberIds' in savedTemplates[0].teams[0], false, 'template excludes team membership');
  assert.equal('participants' in savedTemplates[0], false, 'template excludes player roster');
  assert.equal('roles' in savedTemplates[0], false, 'template excludes role secrets');
  assert.equal('campaign' in savedTemplates[0], false, 'template excludes campaign content');
  const savedRaw = JSON.stringify(savedTemplates[0]);
  assert.equal(savedRaw.includes('不应保存的私密角色'), false);
  assert.equal(savedRaw.includes('周五战役'), false);
  assert.equal(savedRaw.includes('开启北门'), false);

  promptResponses.push('周五引擎');
  await page.getByRole('button', { name: '重命名', exact: true }).click();
  assert.equal(await page.locator('[data-os-user-template] option').first().textContent(), '周五引擎');
  await page.getByRole('button', { name: '关闭' }).click();
  await page.reload({ waitUntil: 'networkidle' });
  await openTableOs(page);
  await editMode(page);
  assert.equal(await page.locator('[data-os-user-template] option').first().textContent(), '周五引擎', 'My Templates persist across reload');

  await page.locator('[data-os-template]').selectOption('universal');
  await page.getByRole('button', { name: '应用模板', exact: true }).click();
  await editMode(page);
  await page.getByRole('button', { name: '应用我的模板', exact: true }).click();
  await page.getByRole('button', { name: '追踪器', exact: true }).click();
  assert.equal(await page.locator('[data-os-tracker-value]').first().inputValue(), '1', 'applying My Template restores initial tracker state, not saved live value');
  const appliedTemplateState = await page.evaluate(() => JSON.parse(localStorage.getItem('board-game-assistant-table-os-v1')));
  assert.ok(String(appliedTemplateState.appliedTemplateId).startsWith('user:'), 'custom template identity persists with the workspace');
  assert.equal(appliedTemplateState.roles.length, 0, 'private roles are not replayed');
  assert.deepEqual(appliedTemplateState.teams[0].memberIds, [], 'team membership is not replayed');
  assert.deepEqual(appliedTemplateState.scoreSheet.values, {}, 'score values are not replayed');
  assert.equal(appliedTemplateState.campaign.name, '周五战役', 'campaign memory survives My Template application');
  assert.equal(appliedTemplateState.campaign.flags[0].name, '开启北门');
  assert.equal(appliedTemplateState.campaign.flags[0].checked, true);

  await editMode(page);
  await page.getByRole('button', { name: '删除模板', exact: true }).click();
  assert.equal(await page.evaluate(() => JSON.parse(localStorage.getItem('board-game-assistant-table-os-user-templates-v1') || '[]').length), 0, 'deleted template is removed only from local template shelf');

'''
          replace_once('tests/e2e/run-table-os-e2e.js', e2e_anchor, e2e_test + e2e_anchor)

          # Docs and release-gate matrix.
          replace_once('TABLE_OS.md',
              "模板只初始化常见阶段、追踪器、团队数量和高级计分字段；用户之后可以自由编辑。\n\n## 状态与存储\n",
              "模板只初始化常见阶段、追踪器、团队数量和高级计分字段；用户之后可以自由编辑。\n\n### My Templates / 我的模板\n\n- 编辑模式可以把当前 Table OS 结构保存为最多 12 个本机模板，并支持重命名、应用和删除。\n- 模板只保存可复用配置：Tracker 的名称/范围/生命周期/上下限/步长/初始值、Phase 名称/备注、Team 名称/颜色和 ScoreSheet 字段/公式，以及是否启用 Campaign 能力。\n- 明确不保存玩家名单、Team 成员、Role/Faction/主持备注、Tracker 当前值、Score 当前值、Phase 当前进度，以及 Campaign 名称/章节/备注/检查点等牌局或战役内容。\n- 应用“我的模板”与内置模板一样保留当前参与者和 Campaign 记忆，不会用旧牌局的私密或瞬时状态覆盖当前桌面。\n- 模板库使用独立 localStorage，仅存在当前设备，不需要账号，也不进入普通 History。\n\n## 状态与存储\n")
          replace_once('TABLE_OS.md',
              "3. 常用自定义配置可以保存为“我的模板”，但模板管理必须保持轻量。\n",
              "3. 如果未来扩展模板分享，只允许显式导出结构配置；默认本机模板继续保持私密、轻量且不进入社区市场。\n")

          replace_once('docs/TABLE_OS_TEST_MATRIX.md',
              "| Node unit tests | roster sync, participant bounds, templates, template-safe campaign persistence, tracker scopes/clamps, session-vs-campaign tracker persistence, phase cycles, team/role reference cleanup, safe formula parsing, score calculations, campaign persistence, serialization/normalization |\n",
              "| Node unit tests | roster sync, participant bounds, built-in templates, privacy-safe My Templates snapshots/apply, template-safe campaign persistence, tracker scopes/clamps, session-vs-campaign tracker persistence, phase cycles, team/role reference cleanup, safe formula parsing, score calculations, campaign persistence, serialization/normalization |\n")
          replace_once('docs/TABLE_OS_TEST_MATRIX.md',
              "| Table OS Chromium E2E | default Play mode, purpose-first quick start, Edit mode, roster sync, universal trackers, phase engine, toolbox-team bridge, two-stage private role reveal, moderator-note isolation, formula score sheet, campaign tracker lifetime, campaign reload persistence, campaign survival across template changes, dynamic bilingual UI |\n",
              "| Table OS Chromium E2E | default Play mode, purpose-first quick start, Edit mode, roster sync, universal trackers, phase engine, toolbox-team bridge, two-stage private role reveal, moderator-note isolation, formula score sheet, campaign tracker lifetime, campaign reload persistence, campaign survival across built-in/My Template changes, local My Templates save/rename/apply/delete with transient/private-data exclusion, dynamic bilingual UI |\n")
          replace_once('docs/TABLE_OS_TEST_MATRIX.md',
              "- Applying a setup template must not erase or silently disable persistent campaign metadata, notes or checkpoints.\n",
              "- Applying a setup template must not erase or silently disable persistent campaign metadata, notes or checkpoints.\n- My Templates must remain local-only structural snapshots: no participants, team membership, roles/factions/moderator notes, live tracker/score values or campaign content may be copied into them.\n")
          PY
      - name: Install dependencies
        run: npm ci
      - name: Install Chromium
        run: npx playwright install --with-deps chromium
      - name: Validate focused migration
        run: npm run check
      - name: Commit product changes and remove migration workflow
        shell: bash
        run: |
          git rm .github/workflows/migrate-my-templates.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/tabletop-core.js src/tabletop.js tests/unit/tabletop-core.test.js tests/e2e/run-table-os-e2e.js TABLE_OS.md docs/TABLE_OS_TEST_MATRIX.md
          git commit -m "feat: add local Table OS templates"
          git push
