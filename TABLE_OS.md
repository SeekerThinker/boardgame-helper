# Table OS 高级桌游助手

Table OS 是桌游助手的高级工作台层，用来覆盖普通计时/计分之外的桌面 bookkeeping：任意状态追踪、阶段流程、团队、私密身份、公式计分表和跨局战役记录。

## 设计原则

1. **牌局优先，配置退后**：默认进入「牌局模式」，只显示正在玩的状态、阶段、身份、分数和检查点；模板、公式、结构编辑、导入导出统一放进「编辑配置」。
2. **不替代实体桌游本身**：棋盘、卡牌、谈判和面对面互动仍留在桌面；数字层只接管容易出错或反复记忆的状态。
3. **离线优先**：全部数据写入设备本地 `localStorage`，不需要账号、云服务或联网。
4. **不破坏原有稳定核心**：Table OS 使用独立状态模型和存储键，与现有计时/计分对局状态解耦；可从当前对局同步玩家名单。
5. **机制优先于单款游戏硬编码**：模板是可编辑的机制型工作流起点，不复制完整规则，也不把产品锁死在某几款游戏上。
6. **隐私默认安全**：玩家私密身份使用两段式 reveal；主持备注永远不进入玩家 reveal 内容。
7. **安全公式**：高级计分公式使用自建算术解析器，只允许变量、数字、四则运算和括号，不调用 `eval` / `Function`，不能执行任意 JavaScript。

## 主牌局生命周期桥接

Table OS 只记录最近确认过的主牌局 `startedAt` 身份。检测到新的非空身份时，companion 显示低噪声提示而不自动重置；用户可选择「开始新一局」，显式应用既有的本局重置策略并同步当前玩家，或选择「继续当前桌面状态」，只确认新身份而不修改桌面数据。第一次关联以及同一主牌局的结束/恢复不会提示；确认后的身份也不会反复触发重置并重复推进战役局数。

## 牌局模式与编辑模式

### 牌局模式（默认）

牌局模式只保留现场真正需要操作的内容：

- Tracker 的大号 +/- 与直接数值输入
- 当前阶段、上一阶段 / 下一阶段与阶段列表
- 团队成员摘要与玩家身份私密查看入口
- 玩家高级计分输入与公式结果
- Campaign 名称、章节、局数、备注和可勾选检查点
- 「新场景 / 下一局」

第一次打开一个尚未配置的 Table OS 时，会先出现「今天需要什么？」的目的型快速入口，而不是先展示所有高级设置。

### 编辑配置

编辑模式集中放置：

- 机制模板选择与应用
- 玩家同步 / 扩展参与者
- Tracker 名称、作用域、步长和生命周期
- Phase 名称与主持备注
- Team 成员编辑、Role / Faction / 主持备注
- ScoreSheet 变量 key、公式、正负方向与是否计入总分
- Campaign 结构编辑
- JSON 导入 / 导出

## 当前能力

### Universal Tracker

- 最多 24 个追踪器。
- 三种作用域：公共、每位参与者、每个团队。
- 每个追踪器支持初始值、最小值、最大值和步长。
- 每个追踪器有两种生命周期：
  - `session` / 本局：开始新场景时回到初始值。
  - `campaign` / 战役：跨场景保留当前值。
- 适合生命、威胁、资源、金币、经验、影响力、声望、Boss HP、公共供应等状态。

Campaign 模板默认把团队资源与经验设为战役级持久，把生命设为本局级状态。

### Phase Engine

- 最多 20 个阶段。
- 可编辑阶段名与主持备注。
- 上一步 / 下一步循环推进。
- 自动维护 cycle 计数，适用于轮次、阶段制和昼夜流程。
- 牌局模式提供「去主计时器」快捷入口，避免在两个工作台之间来回找导航。

### Teams & Private Roles

- Table OS 参与者上限 32，覆盖大型主持型桌游。
- 可从主应用同步当前玩家，也可额外添加只属于高级助手的参与者。
- 最多 8 个团队，成员可自由交叉配置。
- 可一键采用基础工具箱最近一次随机分队结果，不需要重复勾选成员。
- 每位参与者可设置身份、阵营、主持备注和“私密”属性。
- 私密查看采用两段式 pass-the-phone：
  1. 主持人点击「交给玩家查看」后，屏幕只显示对应玩家姓名和交接提示，身份仍然隐藏。
  2. 玩家拿到设备后自行点击「这是我，查看身份」。
  3. reveal 只展示 Role / Faction；主持备注不会被渲染到玩家私密界面。
  4. 玩家点击「看完了」后立即重新遮蔽。

### ScoreSheet v2

- 最多 16 个高级计分栏位。
- 手工栏位与公式栏位可以混合。
- 每个栏位可以正向或负向计入总分，也可以设为“仅显示、不重复计总分”。
- 公式变量通过栏位 key 引用，例如：`base + bonus + objective - penalty`。
- 支持 `+ - * / ( )`；非法 token、函数调用和除零会安全失败为 0，而不会执行代码。
- 牌局模式只显示玩家输入和结果；公式/key 等结构配置只在编辑模式出现。

### Campaign / Legacy Memory

- 可记录战役名称、章节/场景、局数和跨局长备注。
- 最多 40 个检查点 / 解锁项。
- 「新场景 / 下一局」会：
  - 仅重置 `session` Tracker；
  - 保留 `campaign` Tracker；
  - 重置阶段到第一阶段与 cycle 1；
  - 清空私密身份和高级计分值；
  - 保留战役名称、备注和检查点；
  - 战役局数 +1。

### Mechanism Templates

当前内置九组可编辑模板：

- 通用桌游
- 引擎构筑 / 终局计分
- 交易 / 网络 / 经济
- 合作 / 危机管理
- 非对称阵营 / 冲突
- 隐藏身份 / 主持
- 卡牌战斗 / 生命状态
- 战役 / Legacy
- 聚会 / 分队 / 竞猜

模板只初始化常见阶段、追踪器、团队数量和高级计分字段；用户之后可以自由编辑。

## 状态与存储

主应用当前对局仍使用 `board-game-assistant-state-v2`。Table OS 使用独立键：

```text
board-game-assistant-table-os-v1
```

存储 key 保持不变以兼容已经上线的数据；内部 `schemaVersion` 已升级并通过 normalization 为旧数据补默认值。

Table OS 状态包含：

```text
participants
trackers
phases
teams
roles
scoreSheet
campaign
ui
```

参与者可以记录 `sourcePlayerId`，因此从主应用重复同步时，同一位玩家能保持稳定的 Table OS participant id；只属于高级助手的额外参与者不会被同步操作删除。

`ui.mode` 为 `play` 或 `edit`；旧数据默认迁移到 `play`。

## 导入 / 导出

高级助手可以导出 JSON，也可以重新导入。导入时会通过 `normalizeTableOsState()` 清理非法字段、上限和悬空引用，避免损坏状态。

角色和主持备注可能包含隐藏信息，因此导出的 JSON 应由用户自行妥善保存，不建议公开上传。

## 测试门禁

`npm run check` 会覆盖：

- `tests/unit/tabletop-core.test.js`
  - 玩家同步与 32 人上限
  - 模板应用
  - 三类 tracker 与 session/campaign 生命周期
  - phase cycle
  - team / role 引用清理
  - 安全公式解析
  - ScoreSheet v2
  - campaign reset
  - 序列化 / 反序列化
- `tests/e2e/run-table-os-e2e.js`
  - 默认牌局模式和目的型快速开始
  - 编辑模式与主对局玩家同步
  - tracker 实际加减
  - phase 实际推进与主计时器入口
  - 工具箱随机分队 → Table OS Team bridge
  - 两段式私密身份 reveal
  - 主持备注不进入玩家 reveal DOM
  - 公式计分实时结果
  - campaign tracker 生命周期与跨刷新持久化
  - 320px 窄屏、390px 手机与 tablet 无横向溢出
  - 中英文动态切换
- 原有根路径 E2E、子路径 PWA / Service Worker / 离线回归、Android、iOS、商店素材与 Release Candidate 门禁继续保留。

## 后续整合边界

当前优先保持“统一体验，不急着统一存储”。后续只有在能明显减少桌上操作时才继续增加 bridge：

1. Phase Engine 可选绑定主计时器的阶段时长，而不是复制一套计时器。
2. 高级 ScoreSheet 可选把最终总分写入基础结算，而不是再造一个结算页。
3. 历史归档可附带 Table OS 摘要/快照，而不是把完整高级状态塞进基础 schema。
4. 常用自定义配置可以保存为“我的模板”，但模板管理必须保持轻量。

产品原则：**桌游助手应该让玩家少管理东西，而不能变成桌上又多了一个需要管理的东西。**
