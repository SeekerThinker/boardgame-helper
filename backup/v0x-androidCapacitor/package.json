{
  "name": "boardgame-timer",
  "version": "1.0.0",
  "description": "多人桌游计时器",
  "scripts": {
    "build": "echo 'Web app is ready - syncing to Android'",
    "android": "npx cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.3.0",
    "@capacitor/cli": "^8.3.0",
    "@capacitor/core": "^8.3.0"
  }
}
